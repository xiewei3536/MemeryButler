name: Release

on:
  push:
    tags: ['v*']
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-and-release:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4

      - name: Build universal .app and .dmg
        env:
          # 由 tag 決定 App 版本號，確保與 release 一致（見 build.sh 說明）
          VERSION: ${{ github.ref_type == 'tag' && github.ref_name || '' }}
        run: ./build.sh

      - name: Verify universal binary
        run: lipo -info dist/MemoryButler.app/Contents/MacOS/MemoryButler

      - name: Create release and upload DMG
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          TAG="${GITHUB_REF_NAME}"
          gh release create "$TAG" \
            --title "MemoryButler $TAG" \
            --notes-file .github/RELEASE_NOTES.md 2>/dev/null || true
          gh release upload "$TAG" dist/MemoryButler.dmg --clobber
